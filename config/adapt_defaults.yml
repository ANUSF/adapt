common: &common
  adapt.home:             ""
  adapt.is.local:         false
  adapt.db.adapter:       sqlite3
  adapt.db.path:          ""
  adapt.asset.path:       assets
  adapt.config.path:      /home/olaf/etc/adapt/config
  adapt.dir.mode:         0775
  adapt.file.mode:        0660
  adapt.file.ownership:   ""
  ada.openid.server:      http://users.ada.edu.au
  ada.registration.url:   http://users.ada.edu.au/users/new

development:
  <<: *common

stage:
  <<: *common
  adapt.home: adapt_stage

test: &test
  <<: *common
  adapt.asset.path:       tmp/test_assets

deploy: &deploy
  <<: *common
  adapt.home:             /data/httpd/Rails/Adapt/shared
  adapt.config.path:      ""
  adapt.db.adapter:       postgresql
  adapt.file.ownership:   .d10

devs:
  <<: *deploy
  adapt.database:         adapt_devs

staff:
  <<: *deploy
  adapt.database:         adapt_staff
#  adapt.asset.path:       /projects/d10/assda/publish/ANU/Adapt/Production
#  adapt.archive.path:     /projects/d10/assda/publish/ANU/Archive
